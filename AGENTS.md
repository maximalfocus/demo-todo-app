# Team rules for demo-todo-app

- Python standard library only. No new dependencies.
- Every new function gets a unittest test in tests/.
- Run `python3 -m unittest -q` before you open a pull request, and say the result in the PR description.
- Pull request titles start with `[todo]`.
- Keep each pull request to one task.
